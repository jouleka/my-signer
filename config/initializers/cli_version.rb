# frozen_string_literal: true

# Single source of truth for the my-signer CLI version surfaced in the web UI
# (marketing terminals, onboarding, docs). Bump this when a new gem is released.
module MySigner
  CLI_VERSION = "0.1.7"
end
